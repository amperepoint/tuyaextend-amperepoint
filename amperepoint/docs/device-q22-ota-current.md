# Device map: <device_id_q22_ota_current>

## Device

- Device ID: `<device_id_q22_ota_current>`
- HA device name: `Q22 OTA`
- Product/model ID: `cu111poj2mtikvls`
- Product name: `Q22 OTA`
- Category: `qccdz`
- Local IP discovered by TinyTuya broadcast: `<local_ip_q22_ota_current>`
- Local protocol version: `3.5`
- Observed after re-adding the charger to Tuya on 2026-06-11.

This device replaces the earlier test pairing:

```text
<device_id_q22_ota_legacy>
```

The old `tuya-local` entry still points at the previous Device ID/local key and
therefore does not refresh successfully. Re-add the device to `tuya-local` with
the new Device ID, local key and IP before using local LAN values again.

## Cloud DP dump

Current Tuya Cloud / Xtend-visible DPS:

| DP | Code | Raw value | Scale/unit | Meaning |
| --- | --- | --- | --- | --- |
| `1` | `forward_energy_total` | `0` | scale `2`, `kWh` | `0.00 kWh` |
| `3` | `work_state` | `charger_charging` | enum | Charging state |
| `4` | `charge_cur_set` | `16` | `A`, range `6..32` | Current limit |
| `9` | `power_total` | `0` | scale `3`, `kW` | `0.000 kW` |
| `13` | `connection_state` | `controlpi_6v` | enum | Ready to charge / charging control-pilot state |
| `14` | `work_mode` | `charge_now` | enum | Immediate charging |
| `17` | `energy_charge` | `1` | scale `0`, `kWh` | Target energy |
| `18` | `switch` | `true` | bool | Charging enabled |
| `24` | `temp_current` | `31` | `C` | Charger temperature |
| `25` | `charge_energy_once` | `1` | scale `2`, `kWh` | `0.01 kWh` |

Cloud `status_range` and `function` expose only the confirmed DPS above. DP6,
DP7 and DP8 phase values are still not exposed through the current cloud status
API response for this pairing.

## Control instruction mode test

On 2026-06-11 the Tuya Developer project for `Q22 OTA` was switched from
Standard Instruction mode to DP Instruction mode.

Two fresh dumps through the Home Assistant/Tuya Sharing API after the change
still returned the same cloud-visible DPS:

```text
1, 3, 4, 9, 13, 14, 17, 18, 24, 25
```

The following product DPS are still not present in the HA/Tuya Sharing API
status response:

```text
6, 7, 8, 10, 19, 23, 33
```

Interpretation: DP Instruction mode is still useful for Tuya OpenAPI command
semantics, but it did not expose the missing report-only phase DPS through the
Home Assistant/Tuya Sharing API path used by this test.

## Local discovery

TinyTuya broadcast discovery:

```json
{
  "device_id": "<device_id_q22_ota_current>",
  "ip": "<local_ip_q22_ota_current>",
  "product_id": "cu111poj2mtikvls",
  "version": "3.5",
  "origin": "broadcast"
}
```

Local LAN access still requires the new local key generated by Tuya after the
device was re-added.

The local key from the earlier Q22 pairing `<device_id_q22_ota_legacy>` was tested
against the new pairing and does not work:

```text
Err 914: Check device key or version
```

This confirms that the device must be re-added to `tuya-local` with the new
local key before local DPS can be tested.

## Current HACS source mapping

The `AmperePoint Q22 OTA` HACS entry is temporarily mapped to cloud/Xtend
entities so the charger can be observed immediately:

```yaml
model: q22_ota
session_energy_mode: total_delta
source_status: sensor.q22_ota_work_state
source_connected: sensor.q22_ota_connection_state
source_power: sensor.q22_ota_total_power
source_total_energy: sensor.q22_ota_total_energy
source_session_energy: sensor.q22_ota_charge_energy_single
source_current_limit: number.q22_ota_charging_current
source_charge_switch: switch.q22_ota_switch
tariff_value: 1.20
currency: PLN
```

Observed HACS smoke test:

| Entity | State |
| --- | --- |
| `sensor.amperepoint_q22_ota_status` | `Ladowanie` |
| `binary_sensor.amperepoint_q22_ota_auto_podlaczone` | `on` |
| `sensor.amperepoint_q22_ota_moc` | `0.0 kW` |
| `sensor.amperepoint_q22_ota_energia_calkowita` | `0.0 kWh` |
| `sensor.amperepoint_q22_ota_energia_sesji` | `0.0 kWh` |
| `sensor.amperepoint_q22_ota_koszt_sesji` | `0.0 PLN` |
| `number.amperepoint_q22_ota_limit_pradu` | `16 A` |
| `switch.amperepoint_q22_ota_ladowanie` | `on` |

## Notes for DP testing

1. Watch whether DP1 `forward_energy_total` starts increasing during real power
   draw. It is currently `0.00 kWh`.
2. Watch whether DP25 `charge_energy_once` grows live from `0.01/0.02 kWh` or
   latches after a session.
3. DP9 `power_total` is currently `0.000 kW` even while `work_state` is
   `charger_charging`; confirm with a real charging load.
4. DP6/DP7/DP8 phase values are still not visible through cloud status. Re-test
   them after adding the new pairing to `tuya-local`.
