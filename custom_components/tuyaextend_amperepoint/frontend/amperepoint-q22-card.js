const AP_Q22_I18N = {
  en: {
    active: "active",
    activeDraw: "active draw",
    activePhases: "active phases",
    activeSession: "Active session",
    carCp: "Car / CP",
    charging: "Charging",
    code: "Code",
    control: "Control",
    current: "Current",
    currentLimit: "Current limit",
    chargingMode: "Charging mode",
    modeChargeNow: "Charge now",
    modeChargeEnergy: "Charge to energy target",
    modeChargeSchedule: "Scheduled charging",
    modePending: "Waiting for confirmation from Tuya…",
    daily: "Today",
    dataNote: "Control changes are sent through Home Assistant to the charger. Session energy is calculated from the total energy delta.",
    deviceState: "Device state",
    diagnostics: "Diagnostics",
    dp: "DP",
    energy: "Energy",
    fault: "Fault",
    lastSessionDelta: "Last session from delta",
    lastSessionDp25: "Last session from DP25",
    meter: "Meter",
    noData: "No supported datapoints are available yet.",
    noDraw: "no draw",
    noFault: "No faults",
    phaseMeasurements: "Phase measurements",
    power: "Power",
    powerNow: "Power now",
    rawApi: "Raw API",
    rawDp: "Raw DP diagnostics",
    rawHint: "show code / raw API / scaled value",
    readableParameters: "readable parameters",
    ready: "Ready",
    readyPhase: "ready",
    readyToCharge: "Ready to charge",
    scaledMeaning: "Scaled / meaning",
    session: "Session",
    sessionEnergy: "Session energy",
    sessionStart: "Start",
    sessionTime: "Time",
    status: "Status",
    stop: "Stop",
    targetEnergy: "Target energy",
    scheduleStartTime: "From",
    scheduleEndTime: "Until",
    scheduleOvernight: "The window ends the following day.",
    scheduleWholeHours: "The charger supports full hours only.",
    scheduleTitle: "Charger schedule",
    scheduleFallback: "This charger reports a device-specific raw schedule. Configure it in Tuya Smart or Smart Life until its format is recognized.",
    plannerTitle: "Charging planner",
    plannerSubtitle: "Weekly plan with minute precision",
    plannerEnabled: "Planner enabled",
    plannerAddWindow: "Add interval",
    plannerSave: "Save plan",
    plannerNoWindows: "Add at least one charging interval.",
    plannerStart: "Start",
    plannerEnd: "End",
    plannerCurrent: "Current limit",
    plannerDays: "Weekdays",
    plannerRemove: "Remove",
    plannerNext: "Next action",
    plannerNoNext: "No scheduled action",
    plannerStartAction: "Start charging",
    plannerStopAction: "Stop charging",
    plannerCommand: "Tuya command",
    plannerManual: "Manual override",
    plannerCharge30: "Charge 30 min",
    plannerCharge60: "Charge 60 min",
    plannerAddEnergy: "Add energy",
    plannerPause: "Pause until next interval",
    plannerClear: "Return to plan",
    plannerEnergyPlaceholder: "kWh",
    plannerDirty: "Unsaved changes",
    plannerSaved: "Plan saved",
    plannerIdle: "ready",
    plannerPending: "awaiting confirmation",
    plannerConfirmed: "confirmed",
    plannerFailed: "confirmation failed",
    plannerDisabled: "disabled",
    plannerWaiting: "waiting for interval",
    plannerScheduledCharging: "scheduled charging",
    plannerOverrideCharging: "manual charging",
    plannerOverridePaused: "manually paused",
    plannerSourceUnavailable: "Tuya unavailable",
    dayMon: "Mon",
    dayTue: "Tue",
    dayWed: "Wed",
    dayThu: "Thu",
    dayFri: "Fri",
    daySat: "Sat",
    daySun: "Sun",
    temperature: "Temperature",
    voltage: "Voltage",
  },
  pl: {
    active: "aktywna",
    activeDraw: "pobór aktywny",
    activePhases: "aktywne fazy",
    activeSession: "Sesja aktywna",
    carCp: "Auto / CP",
    charging: "Ładowanie",
    code: "Kod",
    control: "Sterowanie",
    current: "Prąd",
    currentLimit: "Limit prądu",
    chargingMode: "Tryb ładowania",
    modeChargeNow: "Ładuj teraz",
    modeChargeEnergy: "Do zadanej energii",
    modeChargeSchedule: "Według harmonogramu",
    modePending: "Oczekiwanie na potwierdzenie z Tuya…",
    daily: "Dzisiaj",
    dataNote: "Zmiany sterowania są wysyłane przez HA do ładowarki. Energia sesji jest liczona z delty licznika całkowitego.",
    deviceState: "Stan urządzenia",
    diagnostics: "Diagnostyka",
    dp: "DP",
    energy: "Energia",
    fault: "Błąd",
    lastSessionDelta: "Ostatnia sesja z delty",
    lastSessionDp25: "Ostatnia sesja z DP25",
    meter: "Licznik",
    noData: "Brak dostępnych obsługiwanych datapointów.",
    noDraw: "bez poboru",
    noFault: "Brak błędów",
    phaseMeasurements: "Pomiary faz",
    power: "Moc",
    powerNow: "Moc teraz",
    rawApi: "Raw API",
    rawDp: "Diagnostyka raw DP",
    rawHint: "pokaż kod / raw API / wartość po skali",
    readableParameters: "czytelne parametry",
    ready: "Gotowe",
    readyPhase: "gotowa",
    readyToCharge: "Gotowy do ładowania",
    scaledMeaning: "Po skali / znaczenie",
    session: "Sesja",
    sessionEnergy: "Energia sesji",
    sessionStart: "Start",
    sessionTime: "Czas",
    status: "Status",
    stop: "Stop",
    targetEnergy: "Energia docelowa",
    scheduleStartTime: "Od",
    scheduleEndTime: "Do",
    scheduleOvernight: "Okno kończy się następnego dnia.",
    scheduleWholeHours: "Ładowarka obsługuje tylko pełne godziny.",
    scheduleTitle: "Harmonogram ładowarki",
    scheduleFallback: "Ta ładowarka raportuje harmonogram w nierozpoznanym formacie. Ustaw go w Tuya Smart lub Smart Life.",
    plannerTitle: "Planer ładowania",
    plannerSubtitle: "Plan tygodniowy z dokładnością do minuty",
    plannerEnabled: "Planer włączony",
    plannerAddWindow: "Dodaj przedział",
    plannerSave: "Zapisz plan",
    plannerNoWindows: "Dodaj co najmniej jeden przedział ładowania.",
    plannerStart: "Start",
    plannerEnd: "Koniec",
    plannerCurrent: "Limit prądu",
    plannerDays: "Dni tygodnia",
    plannerRemove: "Usuń",
    plannerNext: "Następna akcja",
    plannerNoNext: "Brak zaplanowanej akcji",
    plannerStartAction: "Rozpocznij ładowanie",
    plannerStopAction: "Zatrzymaj ładowanie",
    plannerCommand: "Komenda Tuya",
    plannerManual: "Sterowanie ręczne",
    plannerCharge30: "Ładuj 30 min",
    plannerCharge60: "Ładuj 60 min",
    plannerAddEnergy: "Doładuj energię",
    plannerPause: "Wstrzymaj do następnego przedziału",
    plannerClear: "Wróć do planu",
    plannerEnergyPlaceholder: "kWh",
    plannerDirty: "Niezapisane zmiany",
    plannerSaved: "Plan zapisany",
    plannerIdle: "gotowe",
    plannerPending: "oczekuje na potwierdzenie",
    plannerConfirmed: "potwierdzona",
    plannerFailed: "brak potwierdzenia",
    plannerDisabled: "wyłączony",
    plannerWaiting: "oczekuje na przedział",
    plannerScheduledCharging: "ładowanie z planu",
    plannerOverrideCharging: "ładowanie ręczne",
    plannerOverridePaused: "wstrzymany ręcznie",
    plannerSourceUnavailable: "Tuya niedostępna",
    dayMon: "Pon",
    dayTue: "Wt",
    dayWed: "Śr",
    dayThu: "Czw",
    dayFri: "Pt",
    daySat: "Sob",
    daySun: "Nd",
    temperature: "Temperatura",
    voltage: "Napięcie",
  },
  de: {
    active: "aktiv",
    activeDraw: "Verbrauch aktiv",
    activePhases: "aktive Phasen",
    activeSession: "Aktive Sitzung",
    carCp: "Auto / CP",
    charging: "Lädt",
    code: "Code",
    control: "Steuerung",
    current: "Strom",
    currentLimit: "Stromlimit",
    daily: "Heute",
    dataNote: "Änderungen des Limits werden an Home Assistant gesendet. Die Sitzungsenergie wird aus der Differenz des Gesamtzählers berechnet.",
    deviceState: "Gerätestatus",
    diagnostics: "Diagnose",
    dp: "DP",
    energy: "Energie",
    fault: "Fehler",
    lastSessionDelta: "Letzte Sitzung aus Delta",
    lastSessionDp25: "Letzte Sitzung aus DP25",
    meter: "Zähler",
    noData: "Noch keine unterstützten Datenpunkte verfügbar.",
    noDraw: "kein Verbrauch",
    noFault: "Keine Fehler",
    phaseMeasurements: "Phasenmessungen",
    power: "Leistung",
    powerNow: "Aktuelle Leistung",
    rawApi: "Raw API",
    rawDp: "Raw-DP-Diagnose",
    rawHint: "Code / Raw API / skalierten Wert anzeigen",
    readableParameters: "lesbare Parameter",
    ready: "Bereit",
    readyPhase: "bereit",
    readyToCharge: "Bereit zum Laden",
    scaledMeaning: "Skaliert / Bedeutung",
    session: "Sitzung",
    sessionEnergy: "Sitzungsenergie",
    sessionStart: "Start",
    sessionTime: "Zeit",
    status: "Status",
    stop: "Stop",
    temperature: "Temperatur",
    voltage: "Spannung",
  },
  cs: {
    active: "aktivní",
    activeDraw: "aktivní odběr",
    activePhases: "aktivní fáze",
    activeSession: "Aktivní relace",
    carCp: "Auto / CP",
    charging: "Nabíjení",
    code: "Kód",
    control: "Ovládání",
    current: "Proud",
    currentLimit: "Limit proudu",
    daily: "Dnes",
    dataNote: "Změna limitu se odesílá do Home Assistant. Energie relace se počítá z rozdílu celkového elektroměru.",
    deviceState: "Stav zařízení",
    diagnostics: "Diagnostika",
    dp: "DP",
    energy: "Energie",
    fault: "Chyba",
    lastSessionDelta: "Poslední relace z delty",
    lastSessionDp25: "Poslední relace z DP25",
    meter: "Elektroměr",
    noData: "Zatím nejsou dostupné podporované datapointy.",
    noDraw: "bez odběru",
    noFault: "Bez chyb",
    phaseMeasurements: "Měření fází",
    power: "Výkon",
    powerNow: "Aktuální výkon",
    rawApi: "Raw API",
    rawDp: "Diagnostika raw DP",
    rawHint: "zobrazit kód / raw API / hodnotu po škálování",
    readableParameters: "čitelné parametry",
    ready: "Připraveno",
    readyPhase: "připravena",
    readyToCharge: "Připraveno k nabíjení",
    scaledMeaning: "Po škálování / význam",
    session: "Relace",
    sessionEnergy: "Energie relace",
    sessionStart: "Start",
    sessionTime: "Čas",
    status: "Stav",
    stop: "Stop",
    temperature: "Teplota",
    voltage: "Napětí",
  },
  es: {
    active: "activa",
    activeDraw: "consumo activo",
    activePhases: "fases activas",
    activeSession: "Sesión activa",
    carCp: "Coche / CP",
    charging: "Cargando",
    code: "Código",
    control: "Control",
    current: "Corriente",
    currentLimit: "Límite de corriente",
    daily: "Hoy",
    dataNote: "El cambio de límite se envía a Home Assistant. La energía de la sesión se calcula con la delta del contador total.",
    deviceState: "Estado del dispositivo",
    diagnostics: "Diagnóstico",
    dp: "DP",
    energy: "Energía",
    fault: "Error",
    lastSessionDelta: "Última sesión por delta",
    lastSessionDp25: "Última sesión de DP25",
    meter: "Contador",
    noData: "Aún no hay datapoints compatibles disponibles.",
    noDraw: "sin consumo",
    noFault: "Sin errores",
    phaseMeasurements: "Mediciones de fases",
    power: "Potencia",
    powerNow: "Potencia actual",
    rawApi: "Raw API",
    rawDp: "Diagnóstico raw DP",
    rawHint: "mostrar código / raw API / valor escalado",
    readableParameters: "parámetros legibles",
    ready: "Listo",
    readyPhase: "lista",
    readyToCharge: "Listo para cargar",
    scaledMeaning: "Escalado / significado",
    session: "Sesión",
    sessionEnergy: "Energía de sesión",
    sessionStart: "Inicio",
    sessionTime: "Tiempo",
    status: "Estado",
    stop: "Stop",
    temperature: "Temperatura",
    voltage: "Voltaje",
  },
  fr: {
    active: "active",
    activeDraw: "consommation active",
    activePhases: "phases actives",
    activeSession: "Session active",
    carCp: "Voiture / CP",
    charging: "Charge",
    code: "Code",
    control: "Commande",
    current: "Courant",
    currentLimit: "Limite de courant",
    daily: "Aujourd'hui",
    dataNote: "Le changement de limite est envoyé à Home Assistant. L'énergie de session est calculée à partir du delta du compteur total.",
    deviceState: "État de l'appareil",
    diagnostics: "Diagnostic",
    dp: "DP",
    energy: "Énergie",
    fault: "Erreur",
    lastSessionDelta: "Dernière session par delta",
    lastSessionDp25: "Dernière session depuis DP25",
    meter: "Compteur",
    noData: "Aucun datapoint pris en charge n'est encore disponible.",
    noDraw: "pas de consommation",
    noFault: "Aucun défaut",
    phaseMeasurements: "Mesures des phases",
    power: "Puissance",
    powerNow: "Puissance actuelle",
    rawApi: "Raw API",
    rawDp: "Diagnostic raw DP",
    rawHint: "afficher code / raw API / valeur mise à l'échelle",
    readableParameters: "paramètres lisibles",
    ready: "Prêt",
    readyPhase: "prête",
    readyToCharge: "Prêt à charger",
    scaledMeaning: "Mise à l'échelle / signification",
    session: "Session",
    sessionEnergy: "Énergie de session",
    sessionStart: "Début",
    sessionTime: "Temps",
    status: "État",
    stop: "Stop",
    temperature: "Température",
    voltage: "Tension",
  },
  hu: {
    active: "aktív",
    activeDraw: "aktív fogyasztás",
    activePhases: "aktív fázis",
    activeSession: "Aktív munkamenet",
    carCp: "Autó / CP",
    charging: "Töltés",
    code: "Kód",
    control: "Vezérlés",
    current: "Áram",
    currentLimit: "Áramkorlát",
    daily: "Ma",
    dataNote: "A korlát módosítása a Home Assistant felé kerül elküldésre. A munkamenet energiája a teljes mérő delta értékéből számolódik.",
    deviceState: "Eszköz állapota",
    diagnostics: "Diagnosztika",
    dp: "DP",
    energy: "Energia",
    fault: "Hiba",
    lastSessionDelta: "Utolsó munkamenet deltából",
    lastSessionDp25: "Utolsó munkamenet DP25-ből",
    meter: "Mérő",
    noData: "Még nincs elérhető támogatott datapoint.",
    noDraw: "nincs fogyasztás",
    noFault: "Nincs hiba",
    phaseMeasurements: "Fázismérések",
    power: "Teljesítmény",
    powerNow: "Aktuális teljesítmény",
    rawApi: "Raw API",
    rawDp: "Raw DP diagnosztika",
    rawHint: "kód / raw API / skálázott érték megjelenítése",
    readableParameters: "olvasható paraméterek",
    ready: "Kész",
    readyPhase: "kész",
    readyToCharge: "Töltésre kész",
    scaledMeaning: "Skálázott / jelentés",
    session: "Munkamenet",
    sessionEnergy: "Munkamenet energiája",
    sessionStart: "Kezdés",
    sessionTime: "Idő",
    status: "Állapot",
    stop: "Stop",
    temperature: "Hőmérséklet",
    voltage: "Feszültség",
  },
};

const AP_Q22_LOCALES = {
  cs: "cs-CZ",
  de: "de-DE",
  en: "en-US",
  es: "es-ES",
  fr: "fr-FR",
  hu: "hu-HU",
  pl: "pl-PL",
};

class AmperePointQ22Card extends HTMLElement {
  setConfig(config) {
    this.config = {
      title: "AmperePoint",
      subtitle: "AmperePoint Q Series",
      language: "auto",
      maxPowerKw: 22,
      phaseMaxPowerKw: 7.4,
      ...config,
      entities: { ...(config.entities || {}) },
    };
  }

  set hass(hass) {
    this._hass = hass;
    this.applyAutoEntities();
    const modeEntity = this.config?.entities?.chargingMode;
    if (
      this._pendingChargingMode &&
      modeEntity &&
      hass.states?.[modeEntity]?.state === this._pendingChargingMode
    ) {
      this.clearPendingChargingMode();
    }
    this.render();
  }

  getCardSize() {
    return 12;
  }

  applyAutoEntities() {
    if (!this._hass?.states || !this.config) return;
    const detected = this.detectEntities();
    this.config.entities = {
      ...detected.entities,
      ...(this.config.entities || {}),
    };
    if ((!this.config.title || this.config.title === "AmperePoint") && detected.title) {
      this.config.title = detected.title;
    }
  }

  detectEntities() {
    const specs = {
      switch: { domains: ["switch"], any: [" charging", "_charging", " ladowanie", "_switch", " start stop"], not: [] },
      currentLimit: { domains: ["number", "input_number"], any: ["current limit", "current_limit", "charging current", "charging_current", "charge cur set", "charge_cur_set", "limit pradu"], not: ["current_l1", "current_l2", "current_l3"] },
      targetEnergy: { domains: ["number", "input_number"], any: ["target energy", "target_energy", "energy charge", "energy_charge", "energia docelowa"], not: [] },
      chargingMode: { domains: ["select"], any: ["charging mode", "charging_mode", "work mode", "work_mode", "tryb ladowania"], not: [] },
      scheduleStartTime: { domains: ["time"], any: ["schedule starts", "schedule_start_time", "poczatek harmonogramu", "schedule_time"], not: ["end", "koniec"] },
      scheduleEndTime: { domains: ["time"], any: ["schedule ends", "schedule_end_time", "koniec harmonogramu"], not: [] },
      status: { domains: ["sensor"], any: [" status", "_status", "work state", "work_state", "status czytelny"], not: ["connection", "cp"] },
      cp: { domains: ["binary_sensor", "sensor"], any: ["vehicle connected", "vehicle_connected", "connection state", "connection_state", "controlpi", "control pilot", " cp", "_cp"], not: [] },
      faults: { domains: ["sensor", "binary_sensor"], any: [" error", "_error", " fault", "_fault", " bled", "_bled"], not: [] },
      power: { domains: ["sensor"], any: [" power", "_power", "total power", "total_power", "moc chwilowa", "moc teraz"], not: ["power_l1", "power_l2", "power_l3", "moc_l1", "moc_l2", "moc_l3", "phase", "faza"] },
      sessionEnergy: { domains: ["sensor"], any: ["session energy", "session_energy", "energia biezacej sesji", "energia_biezacej_sesji"], not: ["last", "ostatniej", "charge_energy_once"] },
      sessionDuration: { domains: ["sensor"], any: ["session duration", "session_duration", "czas biezacej sesji", "czas_biezacej_sesji"], not: [] },
      totalEnergy: { domains: ["sensor"], any: ["total energy", "total_energy", "forward energy total", "forward_energy_total", "energia calkowita"], not: [] },
      dailyEnergy: { domains: ["sensor"], any: ["daily energy", "daily_energy", "daily total energy", "daily_total_energy", "energia dzienna"], not: [] },
      lastSessionDelta: { domains: ["sensor"], any: ["last session delta", "ostatniej sesji delta", "energia_ostatniej_sesji_delta"], not: [] },
      lastSessionDp25: { domains: ["sensor"], any: ["last session energy", "last_session_energy", "charge energy once", "charge_energy_once", "charge_energy_single", "ostatnia sesja"], not: [] },
      temperature: { domains: ["sensor"], any: ["temperature", "temperatura", "temp current", "temp_current"], not: [] },
      phaseCount: { domains: ["sensor"], any: ["phase count", "phase_count", "liczba faz"], not: [] },
      sessionStart: { domains: ["input_datetime"], any: ["session start", "session_started", "start sesji"], not: [] },
      rawDp: { domains: ["sensor"], any: ["raw dp", "raw_dp", "datapoint", "datapoints"], not: [] },
      planner: { domains: ["sensor"], any: ["charging planner", "planer ladowania", "_planner"], not: [] },
      l1Voltage: { domains: ["sensor"], any: ["voltage_l1", "voltage l1", "napiecie_l1", "phase a voltage", "faza a napiecie"], not: [] },
      l1Current: { domains: ["sensor"], any: ["current_l1", "current l1", "prad_l1", "phase a current", "faza a prad"], not: [] },
      l1Power: { domains: ["sensor"], any: ["power_l1", "power l1", "moc_l1", "phase a power", "faza a moc"], not: [] },
      l2Voltage: { domains: ["sensor"], any: ["voltage_l2", "voltage l2", "napiecie_l2", "phase b voltage", "faza b napiecie"], not: [] },
      l2Current: { domains: ["sensor"], any: ["current_l2", "current l2", "prad_l2", "phase b current", "faza b prad"], not: [] },
      l2Power: { domains: ["sensor"], any: ["power_l2", "power l2", "moc_l2", "phase b power", "faza b moc"], not: [] },
      l3Voltage: { domains: ["sensor"], any: ["voltage_l3", "voltage l3", "napiecie_l3", "phase c voltage", "faza c napiecie"], not: [] },
      l3Current: { domains: ["sensor"], any: ["current_l3", "current l3", "prad_l3", "phase c current", "faza c prad"], not: [] },
      l3Power: { domains: ["sensor"], any: ["power_l3", "power l3", "moc_l3", "phase c power", "faza c moc"], not: [] },
    };
    const detected = {};
    for (const [key, spec] of Object.entries(specs)) {
      detected[key] = this.findEntity(spec);
    }
    Object.keys(detected).forEach((key) => {
      if (!detected[key]) delete detected[key];
    });
    return {
      entities: detected,
      title: this.detectTitle(Object.values(detected).filter(Boolean)),
    };
  }

  findEntity(spec) {
    const prefix = this.normalizeSearch(this.config?.entityPrefix || "");
    const all = Object.keys(this._hass?.states || {}).sort();
    const relevant = all.filter((entityId) => this.isRelevantEntity(entityId, prefix));
    const pools = relevant.length ? [relevant, all] : [all];
    for (const pool of pools) {
      for (const entityId of pool) {
        const domain = entityId.split(".", 1)[0];
        if (spec.domains && !spec.domains.includes(domain)) continue;
        const text = this.entitySearchText(entityId);
        if (prefix && !text.includes(prefix)) continue;
        if (spec.not?.some((item) => text.includes(this.normalizeSearch(item)))) continue;
        if (spec.any?.some((item) => text.includes(this.normalizeSearch(item)))) return entityId;
      }
    }
    return null;
  }

  isRelevantEntity(entityId, prefix) {
    const text = this.entitySearchText(entityId);
    if (prefix && text.includes(prefix)) return true;
    return ["amperepoint", "ampere point", "tuyaextend", "q series", "q11", "q22", "q37", "ev charger", "evse"].some((item) =>
      text.includes(item)
    );
  }

  entitySearchText(entityId) {
    const state = this._hass?.states?.[entityId];
    return this.normalizeSearch(`${entityId} ${state?.attributes?.friendly_name || ""}`);
  }

  normalizeSearch(value) {
    return String(value || "")
      .toLowerCase()
      .normalize("NFD")
      .replace(/[\u0300-\u036f]/g, "")
      .replace(/[_\-.]+/g, " ")
      .replace(/\s+/g, " ")
      .trim();
  }

  detectTitle(entityIds) {
    for (const entityId of entityIds) {
      const name = this._hass?.states?.[entityId]?.attributes?.friendly_name;
      if (!name) continue;
      const clean = String(name)
        .replace(/\b(Status|Power|Moc|Energia|Session energy|Total energy|Temperature|Temperatura)\b.*$/i, "")
        .trim();
      if (clean) return clean;
    }
    return null;
  }

  lang() {
    const configured = this.config?.language;
    const raw =
      configured && configured !== "auto"
        ? configured
        : this._hass?.language || this._hass?.locale?.language || navigator.language || "en";
    const base = String(raw).toLowerCase().split(/[-_]/)[0];
    const normalized = base === "cz" ? "cs" : base;
    return AP_Q22_I18N[normalized] ? normalized : "en";
  }

  t(key) {
    const language = this.lang();
    return AP_Q22_I18N[language]?.[key] || AP_Q22_I18N.en[key] || key;
  }

  locale() {
    return AP_Q22_LOCALES[this.lang()] || AP_Q22_LOCALES.en;
  }

  stateObj(entity) {
    return this._hass?.states?.[entity] || null;
  }

  isMissingValue(value) {
    return value === undefined || value === null || value === "" || value === "unknown" || value === "unavailable";
  }

  hasEntity(entity) {
    const value = this.stateObj(entity)?.state;
    return !this.isMissingValue(value);
  }

  hasAttr(entity, name) {
    const value = this.stateObj(entity)?.attributes?.[name];
    return !this.isMissingValue(value);
  }

  state(entity, fallback = null) {
    const value = this.stateObj(entity)?.state;
    return this.isMissingValue(value) ? fallback : value;
  }

  attr(entity, name, fallback = null) {
    const value = this.stateObj(entity)?.attributes?.[name];
    return this.isMissingValue(value) ? fallback : value;
  }

  num(entity, fallback = 0) {
    const parsed = Number(String(this.state(entity, fallback)).replace(",", "."));
    return Number.isFinite(parsed) ? parsed : fallback;
  }

  fmt(value, unit = "", digits = 1) {
    if (this.isMissingValue(value)) return null;
    const parsed = Number(String(value).replace(",", "."));
    if (!Number.isFinite(parsed)) return `${this.escape(value)}${unit ? ` ${unit}` : ""}`;
    return `${parsed.toFixed(digits)}${unit ? ` ${unit}` : ""}`;
  }

  escape(value) {
    return String(value)
      .replaceAll("&", "&amp;")
      .replaceAll("<", "&lt;")
      .replaceAll(">", "&gt;")
      .replaceAll('"', "&quot;")
      .replaceAll("'", "&#039;");
  }

  normalize(value) {
    return String(value || "")
      .toLowerCase()
      .normalize("NFD")
      .replace(/[\u0300-\u036f]/g, "")
      .replace(/[_-]/g, " ")
      .replace(/\s+/g, " ")
      .trim();
  }

  human(value) {
    if (this.isMissingValue(value)) return null;
    const normalized = this.normalize(value);
    if (["ladowanie", "ladownie", "charging", "charger charging", "charger is charging"].includes(normalized)) {
      return this.t("charging");
    }
    if (["gotowy do ladowania", "ready to charge", "controlpi 6v", "control pilot 6v", "6v"].includes(normalized)) {
      return this.t("readyToCharge");
    }
    if (["brak bledow", "no faults", "no fault", "0", "false"].includes(normalized)) {
      return this.t("noFault");
    }
    return this.escape(value);
  }

  formatDate(entity) {
    const raw = this.state(entity, null);
    if (!raw) return null;
    const normalized = String(raw).includes("T") ? raw : String(raw).replace(" ", "T");
    const date = new Date(normalized);
    if (Number.isNaN(date.getTime())) return this.escape(raw);
    return date.toLocaleString(this.locale(), {
      day: "2-digit",
      month: "2-digit",
      hour: "2-digit",
      minute: "2-digit",
    });
  }

  isCharging() {
    const status = this.normalize(this.state(this.config.entities.status, ""));
    const power = this.num(this.config.entities.power, 0);
    const chargingStates = ["ladowanie", "ladownie", "charging", "charger charging", "charger is charging"];
    return power > 0.1 || chargingStates.includes(status);
  }

  async toggleCharging() {
    const planner = this.stateObj(this.config.entities.planner);
    if (planner?.attributes?.enabled) {
      await this.setPlannerOverride(this.isCharging() ? "pause" : "charge", {
        duration_minutes: 60,
      });
      return;
    }
    const entityId = this.config.entities.switch;
    if (!this.hasEntity(entityId)) return;
    const isOn = this.state(entityId) === "on";
    await this._hass.callService("switch", isOn ? "turn_off" : "turn_on", { entity_id: entityId });
  }

  async setCurrentLimit(value) {
    const entityId = this.config.entities.currentLimit;
    if (!this.hasEntity(entityId)) return;
    await this._hass.callService("number", "set_value", { entity_id: entityId, value: Number(value) });
  }

  async setChargingMode(value) {
    const entityId = this.config.entities.chargingMode;
    if (!this.hasEntity(entityId)) return;
    this._pendingChargingMode = value;
    clearTimeout(this._pendingChargingModeTimer);
    this._pendingChargingModeTimer = setTimeout(() => {
      this.clearPendingChargingMode();
      this.render();
    }, 30_000);
    this.render();
    try {
      await this._hass.callService("select", "select_option", { entity_id: entityId, option: value });
    } catch (error) {
      this.clearPendingChargingMode();
      this.render();
      throw error;
    }
  }

  clearPendingChargingMode() {
    this._pendingChargingMode = null;
    clearTimeout(this._pendingChargingModeTimer);
    this._pendingChargingModeTimer = null;
  }

  async setTargetEnergy(value) {
    const entityId = this.config.entities.targetEnergy;
    if (!this.hasEntity(entityId)) return;
    await this._hass.callService("number", "set_value", { entity_id: entityId, value: Number(value) });
  }

  async setScheduleBoundary(entityId, value) {
    if (!this.hasEntity(entityId) || !value) return;
    await this._hass.callService("time", "set_value", { entity_id: entityId, time: `${value}:00` });
  }

  syncPlannerDraft(attributes) {
    const source = JSON.stringify([Boolean(attributes.enabled), attributes.windows || []]);
    if (this._plannerDirty || this._plannerSource === source) return;
    this._plannerSource = source;
    this._plannerDraft = {
      enabled: Boolean(attributes.enabled),
      windows: JSON.parse(JSON.stringify(attributes.windows || [])),
    };
  }

  plannerEntryId() {
    return this.stateObj(this.config.entities.planner)?.attributes?.config_entry_id;
  }

  async savePlanner() {
    const configEntryId = this.plannerEntryId();
    if (!configEntryId || !this._plannerDraft) return;
    await this._hass.callService("tuyaextend_amperepoint", "set_planner", {
      config_entry_id: configEntryId,
      enabled: Boolean(this._plannerDraft.enabled),
      windows: this._plannerDraft.windows,
    });
    this._plannerDirty = false;
    this._plannerSaved = true;
    this.render();
  }

  async setPlannerOverride(mode, values = {}) {
    const configEntryId = this.plannerEntryId();
    if (!configEntryId) return;
    await this._hass.callService("tuyaextend_amperepoint", "set_planner_override", {
      config_entry_id: configEntryId,
      mode,
      ...values,
    });
  }

  addPlannerWindow() {
    if (!this._plannerDraft) return;
    this._plannerDraft.windows.push({
      id: `window-${Date.now()}`,
      days: [0, 1, 2, 3, 4, 5, 6],
      start: "22:00",
      end: "06:00",
      current_a: 16,
      priority: 0,
    });
    this._plannerDirty = true;
    this._plannerSaved = false;
    this.render();
  }

  plannerStateLabel(value) {
    const key = {
      disabled: "plannerDisabled",
      waiting: "plannerWaiting",
      scheduled_charging: "plannerScheduledCharging",
      override_charging: "plannerOverrideCharging",
      override_paused: "plannerOverridePaused",
      source_unavailable: "plannerSourceUnavailable",
      pending: "plannerPending",
      failed: "plannerFailed",
    }[value];
    return this.t(key || "plannerIdle");
  }

  plannerCommandLabel(value) {
    return this.t(
      {
        pending: "plannerPending",
        confirmed: "plannerConfirmed",
        failed: "plannerFailed",
        idle: "plannerIdle",
      }[value] || "plannerIdle"
    );
  }

  formatPlannerDate(value) {
    const date = new Date(value);
    if (Number.isNaN(date.getTime())) return this.escape(value || "-");
    return date.toLocaleString(this.locale(), {
      weekday: "short",
      hour: "2-digit",
      minute: "2-digit",
    });
  }

  renderPlannerCard(attributes, minCurrent, maxCurrent) {
    this.syncPlannerDraft(attributes);
    const draft = this._plannerDraft || { enabled: false, windows: [] };
    const dayKeys = ["dayMon", "dayTue", "dayWed", "dayThu", "dayFri", "daySat", "daySun"];
    const next = attributes.next_action;
    const nextText = next
      ? `${this.t(next.action === "start" ? "plannerStartAction" : "plannerStopAction")} · ${this.formatPlannerDate(next.at)}`
      : this.t("plannerNoNext");
    const state = this.state(this.config.entities.planner, "disabled");
    const rows = draft.windows
      .map(
        (window, index) => `
          <div class="planner-window" data-window="${index}">
            <div class="planner-days" aria-label="${this.t("plannerDays")}">
              ${dayKeys
                .map(
                  (key, day) => `<label class="day-chip ${window.days?.includes(day) ? "selected" : ""}">
                    <input type="checkbox" data-planner-day="${day}" ${window.days?.includes(day) ? "checked" : ""} />
                    <span>${this.t(key)}</span>
                  </label>`
                )
                .join("")}
            </div>
            <label class="planner-field"><span>${this.t("plannerStart")}</span><input data-planner-field="start" type="time" step="60" value="${this.escape(window.start)}" /></label>
            <label class="planner-field"><span>${this.t("plannerEnd")}</span><input data-planner-field="end" type="time" step="60" value="${this.escape(window.end)}" /></label>
            <label class="planner-field"><span>${this.t("plannerCurrent")}</span><div class="planner-number"><input data-planner-field="current_a" type="number" min="${minCurrent}" max="${maxCurrent}" step="1" value="${this.escape(window.current_a)}" /><b>A</b></div></label>
            <button class="planner-remove" type="button" title="${this.t("plannerRemove")}">${this.icon("mdi:trash-can-outline")}</button>
          </div>`
      )
      .join("");

    return `
      <section class="planner-card">
        <div class="planner-head">
          <div>
            <span>${this.t("plannerSubtitle")}</span>
            <strong>${this.t("plannerTitle")}</strong>
          </div>
          <span class="planner-state ${this.escape(state)}">${this.plannerStateLabel(state)}</span>
        </div>
        <div class="planner-summary">
          <div>${this.icon("mdi:calendar-arrow-right")}<span><small>${this.t("plannerNext")}</small><b>${nextText}</b></span></div>
          <div>${this.icon("mdi:cloud-check-outline")}<span><small>${this.t("plannerCommand")}</small><b>${this.plannerCommandLabel(attributes.command_status)}</b></span></div>
        </div>
        <label class="planner-toggle"><input class="planner-enabled" type="checkbox" ${draft.enabled ? "checked" : ""} /><span>${this.t("plannerEnabled")}</span></label>
        <div class="planner-windows">${rows || `<div class="planner-empty">${this.t("plannerNoWindows")}</div>`}</div>
        <div class="planner-actions">
          <button class="planner-add" type="button">${this.icon("mdi:plus")} ${this.t("plannerAddWindow")}</button>
          <span class="planner-save-state">${this._plannerDirty ? this.t("plannerDirty") : this._plannerSaved ? this.t("plannerSaved") : ""}</span>
          <button class="planner-save" type="button">${this.icon("mdi:content-save-outline")} ${this.t("plannerSave")}</button>
        </div>
        <div class="planner-override">
          <div><span>${this.t("plannerManual")}</span></div>
          <button type="button" data-planner-override="charge" data-duration="30">${this.t("plannerCharge30")}</button>
          <button type="button" data-planner-override="charge" data-duration="60">${this.t("plannerCharge60")}</button>
          <div class="planner-energy"><input class="planner-energy-value" type="number" min="0.1" max="200" step="0.1" value="10" aria-label="${this.t("plannerEnergyPlaceholder")}" /><b>kWh</b><button type="button" data-planner-override="energy">${this.t("plannerAddEnergy")}</button></div>
          <button type="button" data-planner-override="pause">${this.t("plannerPause")}</button>
          <button type="button" data-planner-override="clear">${this.t("plannerClear")}</button>
        </div>
      </section>`;
  }

  chargingModeKind(value) {
    const normalized = this.normalize(value);
    if (["charge energy", "fixed charge", "fixed_charge"].includes(normalized)) return "energy";
    if (["charge schedule", "scheduled charge", "scheduled_charge"].includes(normalized)) return "schedule";
    return "now";
  }

  chargingModeLabel(value) {
    const kind = this.chargingModeKind(value);
    if (kind === "energy") return this.t("modeChargeEnergy");
    if (kind === "schedule") return this.t("modeChargeSchedule");
    return this.t("modeChargeNow");
  }

  icon(name) {
    return `<ha-icon icon="${name}"></ha-icon>`;
  }

  metric(labelKey, entity, unit, digits, icon, tone = "") {
    if (!this.hasEntity(entity)) return "";
    const value = this.fmt(this.state(entity), "", digits);
    return `
      <div class="metric ${tone}">
        <div class="metric-icon">${this.icon(icon)}</div>
        <div>
          <div class="metric-label">${this.t(labelKey)}</div>
          <div class="metric-value">${value}<span>${unit}</span></div>
        </div>
      </div>
    `;
  }

  phaseData() {
    const e = this.config.entities;
    return [
      { label: "L1", voltage: e.l1Voltage, current: e.l1Current, power: e.l1Power },
      { label: "L2", voltage: e.l2Voltage, current: e.l2Current, power: e.l2Power },
      { label: "L3", voltage: e.l3Voltage, current: e.l3Current, power: e.l3Power },
    ].filter((phase) => this.hasEntity(phase.voltage) || this.hasEntity(phase.current) || this.hasEntity(phase.power));
  }

  phase(phase) {
    const voltage = this.hasEntity(phase.voltage) ? this.num(phase.voltage) : null;
    const current = this.hasEntity(phase.current) ? this.num(phase.current) : null;
    const power = this.hasEntity(phase.power) ? this.num(phase.power) : null;
    const active = (current || 0) > 0.2 || (power || 0) > 0.05;
    const width = power === null ? 2 : Math.max(2, Math.min(100, (power / this.config.phaseMaxPowerKw) * 100));
    const cells = [
      voltage !== null ? `<div><span>${this.t("voltage")}</span><b>${this.fmt(voltage, "V", 0)}</b></div>` : "",
      current !== null ? `<div><span>${this.t("current")}</span><b>${this.fmt(current, "A", 1)}</b></div>` : "",
    ].join("");
    return `
      <div class="phase ${active ? "active" : ""}">
        <div class="phase-head">
          <div>
            <strong>${phase.label}</strong>
            <span>${active ? this.t("active") : this.t("readyPhase")}</span>
          </div>
          ${power !== null ? `<b>${this.fmt(power, "kW", 2)}</b>` : ""}
        </div>
        ${power !== null ? `<div class="bar"><i style="width:${width}%"></i></div>` : ""}
        ${cells ? `<div class="phase-grid">${cells}</div>` : ""}
      </div>
    `;
  }

  statusRow(icon, labelKey, value, tone = "") {
    if (this.isMissingValue(value)) return "";
    return `
      <div class="status-row ${tone}">
        <div class="row-icon">${this.icon(icon)}</div>
        <span>${this.t(labelKey)}</span>
        <strong>${value}</strong>
      </div>
    `;
  }

  rawRows() {
    const raw = this.config.entities.rawDp;
    const snapshot = this.attr(raw, "raw_dp", null);
    const metadata = this.attr(raw, "dp_metadata", {}) || {};
    const knownDpIds = {
      forward_energy_total: 1,
      work_state: 3,
      charge_cur_set: 4,
      phase_a: 6,
      phase_b: 7,
      phase_c: 8,
      power_total: 9,
      fault: 10,
      connection_state: 13,
      work_mode: 14,
      energy_charge: 17,
      switch: 18,
      local_timer: 19,
      system_version: 23,
      temp_current: 24,
      charge_energy_once: 25,
      mode_set: 33,
    };

    if (
      snapshot &&
      typeof snapshot === "object" &&
      !Array.isArray(snapshot) &&
      Object.keys(snapshot).length > 0
    ) {
      return Object.entries(snapshot)
        .map(([code, value]) => {
          const definition = metadata[code] || {};
          return {
            code,
            value,
            definition,
            dp: definition.dp_id ?? knownDpIds[code] ?? "–",
          };
        })
        .sort((a, b) => {
          const aDp = typeof a.dp === "number" ? a.dp : Number.MAX_SAFE_INTEGER;
          const bDp = typeof b.dp === "number" ? b.dp : Number.MAX_SAFE_INTEGER;
          return aDp - bDp || a.code.localeCompare(b.code);
        })
        .map(({ code, value, definition, dp }) => {
          let decoded = value;
          if (typeof value === "number" && Number(definition.scale || 0) > 0) {
            decoded = value / 10 ** Number(definition.scale);
          }
          if (typeof value === "boolean") decoded = value ? "true" : "false";
          if (typeof decoded === "object") decoded = JSON.stringify(decoded);
          const unit = definition.unit ? ` ${this.escape(definition.unit)}` : "";
          const access = definition.writable ? " ↔" : "";
          return `
            <tr>
              <td>${this.escape(dp)}</td>
              <td><code>${this.escape(code)}</code>${access}</td>
              <td><code>${this.escape(typeof value === "object" ? JSON.stringify(value) : value)}</code></td>
              <td>${this.escape(decoded)}${unit}</td>
            </tr>
          `;
        })
        .join("");
    }

    const rowDefs = [
      ["1", "forward_energy_total", "raw_forward_energy_total", this.attr(raw, "forward_energy_total_kwh"), "kWh"],
      ["3", "work_state", "raw_work_state", this.human(this.state(this.config.entities.status)), ""],
      ["4", "charge_cur_set", "raw_charge_cur_set", this.attr(raw, "charge_current_a"), "A"],
      ["6", "phase_a", "raw_phase_a", this.phaseRawText(raw, "phase_a"), ""],
      ["7", "phase_b", "raw_phase_b", this.phaseRawText(raw, "phase_b"), ""],
      ["8", "phase_c", "raw_phase_c", this.phaseRawText(raw, "phase_c"), ""],
      ["9", "power_total", "raw_power_total", this.attr(raw, "power_total_kw"), "kW"],
      ["10", "fault", "raw_fault", this.human(this.state(this.config.entities.faults)), ""],
      ["13", "connection_state", "raw_connection_state", this.human(this.state(this.config.entities.cp)), ""],
      ["17", "energy_charge", "raw_energy_charge", this.attr(raw, "energy_charge_kwh"), "kWh"],
      ["18", "switch", "raw_switch", this.state(this.config.entities.switch), ""],
      ["24", "temp_current", "raw_temp_current", this.attr(raw, "temp_current_c"), "C"],
      ["25", "charge_energy_once", "raw_charge_energy_once", this.attr(raw, "charge_energy_once_kwh"), "kWh"],
    ];

    return rowDefs
      .filter(([, , rawKey, decoded]) => this.hasAttr(raw, rawKey) || !this.isMissingValue(decoded))
      .map(([dp, code, rawKey, decoded, unit]) => {
        const rawValue = this.attr(raw, rawKey, null);
        const decodedText = this.isMissingValue(decoded) ? "" : `${this.escape(decoded)}${unit ? ` ${unit}` : ""}`;
        return `
          <tr>
            <td>${dp}</td>
            <td><code>${code}</code></td>
            <td>${rawValue === null ? "" : `<code>${this.escape(rawValue)}</code>`}</td>
            <td>${decodedText}</td>
          </tr>
        `;
      })
      .join("");
  }

  phaseRawText(raw, prefix) {
    const voltage = this.attr(raw, `${prefix}_voltage_v`, null);
    const current = this.attr(raw, `${prefix}_current_a`, null);
    const power = this.attr(raw, `${prefix}_power_kw`, null);
    if (this.isMissingValue(voltage) && this.isMissingValue(current) && this.isMissingValue(power)) return null;
    return `${voltage ?? "-"} V / ${current ?? "-"} A / ${power ?? "-"} kW`;
  }

  render() {
    if (!this.config || !this._hass) return;

    const e = this.config.entities;
    const powerAvailable = this.hasEntity(e.power);
    const power = this.num(e.power);
    const powerPct = powerAvailable ? Math.max(2, Math.min(100, (power / this.config.maxPowerKw) * 100)) : 0;
    const powerArc = Math.min(78, powerPct * 0.78);
    const charging = this.isCharging();
    const currentEntity = this.stateObj(e.currentLimit);
    const hasCurrentLimit = this.hasEntity(e.currentLimit);
    const hasSwitch = this.hasEntity(e.switch);
    const chargingEnabled = hasSwitch && this.state(e.switch) === "on";
    const chargingModeEntity = this.stateObj(e.chargingMode);
    const hasChargingMode = this.hasEntity(e.chargingMode);
    const chargingModes = chargingModeEntity?.attributes?.options || [];
    const chargingMode = this._pendingChargingMode || this.state(e.chargingMode, "charge_now");
    const chargingModeKind = this.chargingModeKind(chargingMode);
    const targetEnergyEntity = this.stateObj(e.targetEnergy);
    const hasTargetEnergy = this.hasEntity(e.targetEnergy);
    const showTargetEnergy = hasTargetEnergy && chargingModeKind === "energy";
    const scheduleStartEntity = e.scheduleStartTime || e.scheduleTime;
    const scheduleEndEntity = e.scheduleEndTime;
    const hasScheduleStart = this.hasEntity(scheduleStartEntity);
    const hasScheduleEnd = this.hasEntity(scheduleEndEntity);
    const hasScheduleWindow = hasScheduleStart && hasScheduleEnd;
    const showSchedule = hasChargingMode && chargingModeKind === "schedule";
    const scheduleStartTime = hasScheduleStart ? String(this.state(scheduleStartEntity)).slice(0, 5) : "";
    const scheduleEndTime = hasScheduleEnd ? String(this.state(scheduleEndEntity)).slice(0, 5) : "";
    const scheduleCrossesMidnight =
      hasScheduleWindow && Number(scheduleEndTime.slice(0, 2)) < Number(scheduleStartTime.slice(0, 2));
    const current = this.num(e.currentLimit, 6);
    const minCurrent = Number(currentEntity?.attributes?.min ?? 6);
    const maxCurrent = Number(currentEntity?.attributes?.max ?? 32);
    const stepCurrent = Number(currentEntity?.attributes?.step ?? 1);
    const plannerEntity = this.stateObj(e.planner);
    const plannerCard = plannerEntity
      ? this.renderPlannerCard(plannerEntity.attributes || {}, minCurrent, maxCurrent)
      : "";
    const faults = this.human(this.state(e.faults));
    const hasFault = faults && faults !== this.t("noFault") && faults !== "0";
    const phases = this.phaseData();
    const rawRows = this.rawRows();
    const hasRaw = rawRows.length > 0 && this.hasEntity(e.rawDp);
    const heroMeta = [
      this.hasEntity(e.cp) ? `<span>${this.icon("mdi:ev-station")} ${this.human(this.state(e.cp))}</span>` : "",
      phases.length ? `<span>${this.icon("mdi:sine-wave")} ${phases.length} ${this.t("activePhases")}</span>` : "",
      this.hasEntity(e.temperature) ? `<span>${this.icon("mdi:thermometer")} ${this.fmt(this.state(e.temperature), "C", 0)}</span>` : "",
    ].join("");

    const powerCard = powerAvailable
      ? `
        <div class="power-card">
          <div class="gauge" style="--power:${powerArc}%;">
            <div>
              <strong>${this.fmt(power, "", 2)}</strong>
              <span>kW ${this.t("powerNow").toLowerCase()}</span>
            </div>
          </div>
          <div class="session-strip">
            ${this.hasEntity(e.sessionEnergy) ? `<div><span>${this.t("sessionEnergy")}</span><b>${this.fmt(this.state(e.sessionEnergy), "kWh", 2)}</b></div>` : ""}
            ${this.hasEntity(e.sessionDuration) ? `<div><span>${this.t("sessionTime")}</span><b>${this.escape(this.state(e.sessionDuration))} min</b></div>` : ""}
            ${this.hasEntity(e.sessionStart) ? `<div><span>${this.t("sessionStart")}</span><b>${this.formatDate(e.sessionStart)}</b></div>` : ""}
          </div>
        </div>
      `
      : "";

    const controlCard =
      hasSwitch || hasCurrentLimit || hasChargingMode || showTargetEnergy || showSchedule
        ? `
        <div class="control-card">
          <div class="card-title">
            <div>
              <span>${this.t("control")}</span>
              <strong>${charging ? this.t("activeSession") : this.t("ready")}</strong>
            </div>
            ${
              hasSwitch
                ? `<button class="power-button ${chargingEnabled ? "on" : ""}" type="button">
                    ${this.icon(chargingEnabled ? "mdi:pause" : "mdi:play")}
                    ${chargingEnabled ? this.t("stop") : this.t("charging")}
                  </button>`
                : ""
            }
          </div>
          ${
            hasCurrentLimit
              ? `
                <label class="slider-label">
                  <span>${this.t("currentLimit")}</span>
                  <b>${current} A</b>
                </label>
                <input class="current-slider" type="range" min="${minCurrent}" max="${maxCurrent}" step="${stepCurrent}" value="${current}" />
                <div class="slider-scale"><span>${minCurrent} A</span><span>${maxCurrent} A</span></div>
              `
              : ""
          }
          <div class="control-fields ${showTargetEnergy ? "" : "single"}">
            ${
              hasChargingMode
                ? `<label><span>${this.t("chargingMode")}</span><select class="charging-mode" ${this._pendingChargingMode ? "disabled" : ""}>${chargingModes
                    .map((option) => `<option value="${this.escape(option)}" ${option === chargingMode ? "selected" : ""}>${this.escape(this.chargingModeLabel(option))}</option>`)
                    .join("")}</select></label>`
                : ""
            }
            ${
              showTargetEnergy
                ? `<label><span>${this.t("targetEnergy")}</span><div class="number-field"><input class="target-energy" type="number" min="${targetEnergyEntity?.attributes?.min ?? 0}" max="${targetEnergyEntity?.attributes?.max ?? 200}" step="${targetEnergyEntity?.attributes?.step ?? 1}" value="${this.escape(this.state(e.targetEnergy))}" /><b>kWh</b></div></label>`
                : ""
            }
          </div>
          ${this._pendingChargingMode ? `<div class="mode-pending">${this.icon("mdi:cloud-sync")} ${this.t("modePending")}</div>` : ""}
          ${
            showSchedule
              ? `<div class="schedule-card">
                  ${this.icon("mdi:calendar-clock")}
                  <div>
                    <strong>${this.t("scheduleTitle")}</strong>
                    ${
                      hasScheduleWindow
                        ? `<div class="schedule-window">
                            <label><span>${this.t("scheduleStartTime")}</span><input class="schedule-start-time" type="time" step="3600" value="${this.escape(scheduleStartTime)}" /></label>
                            <label><span>${this.t("scheduleEndTime")}</span><input class="schedule-end-time" type="time" step="3600" value="${this.escape(scheduleEndTime)}" /></label>
                          </div>
                          <span>${this.t("scheduleWholeHours")}</span>
                          ${scheduleCrossesMidnight ? `<span class="schedule-overnight">${this.t("scheduleOvernight")}</span>` : ""}`
                        : `<span>${this.t("scheduleFallback")}</span>`
                    }
                  </div>
                </div>`
              : ""
          }
          ${
            hasCurrentLimit || hasChargingMode || showTargetEnergy || showSchedule
              ? `<div class="control-note">
                  ${this.icon("mdi:shield-check")}
                  <span>${this.t("dataNote")}</span>
                </div>`
              : ""
          }
        </div>
      `
        : "";

    const metrics = [
      this.metric("power", e.power, "kW", 2, "mdi:flash", "accent"),
      this.metric("session", e.sessionEnergy, "kWh", 2, "mdi:battery-charging"),
      this.metric("daily", e.dailyEnergy, "kWh", 2, "mdi:calendar-today"),
      this.metric("meter", e.totalEnergy, "kWh", 1, "mdi:counter"),
    ].join("");

    const phasePanel = phases.length
      ? `
        <div class="panel phases-panel">
          <div class="panel-head">
            <div>
              <span>${this.t("phaseMeasurements")}</span>
              <strong>${phases.map((phase) => phase.label).join(" / ")}</strong>
            </div>
            <em>${power > 0.1 ? this.t("activeDraw") : this.t("noDraw")}</em>
          </div>
          ${phases.map((phase) => this.phase(phase)).join("")}
        </div>
      `
      : "";

    const statusRows = [
      this.statusRow("mdi:state-machine", "status", this.human(this.state(e.status)), charging ? "good" : ""),
      this.statusRow("mdi:car-electric", "carCp", this.human(this.state(e.cp))),
      this.statusRow("mdi:alert-circle-outline", "diagnostics", faults, hasFault ? "bad" : "good"),
      this.statusRow("mdi:thermometer", "temperature", this.hasEntity(e.temperature) ? this.fmt(this.state(e.temperature), "C", 0) : null),
      this.statusRow("mdi:history", "lastSessionDp25", this.hasEntity(e.lastSessionDp25) ? this.fmt(this.state(e.lastSessionDp25), "kWh", 2) : null),
      this.statusRow("mdi:delta", "lastSessionDelta", this.hasEntity(e.lastSessionDelta) ? this.fmt(this.state(e.lastSessionDelta), "kWh", 2) : null),
    ].join("");

    const statusPanel = statusRows
      ? `
        <div class="panel">
          <div class="panel-head">
            <div>
              <span>${this.t("deviceState")}</span>
              <strong>${this.t("readableParameters")}</strong>
            </div>
          </div>
          ${statusRows}
        </div>
      `
      : "";

    const contentPanels = [phasePanel, statusPanel].filter(Boolean);
    const hasAnyData = powerCard || controlCard || plannerCard || metrics || contentPanels.length || hasRaw;

    this.innerHTML = `
      <ha-card>
        <div class="app">
          <section class="hero">
            <div class="hero-main">
              <div class="eyebrow">${this.escape(this.config.subtitle)}</div>
              <h1>${this.escape(this.config.title)}</h1>
              ${heroMeta ? `<div class="hero-meta">${heroMeta}</div>` : ""}
            </div>
            <div class="hero-status">
              ${
                this.hasEntity(e.status) || charging
                  ? `<span class="pill ${charging ? "charging" : "idle"}">${charging ? this.t("charging") : this.human(this.state(e.status))}</span>`
                  : ""
              }
              ${this.hasEntity(e.rawDp) ? `<small>${this.t("dp")}: ${this.escape(this.state(e.rawDp))}</small>` : ""}
            </div>
          </section>

          ${
            hasAnyData
              ? `
                <section class="dashboard ${!powerCard || !controlCard ? "single" : ""}">
                  ${powerCard}
                  ${controlCard}
                  ${metrics ? `<div class="metrics-grid">${metrics}</div>` : ""}
                </section>
                ${plannerCard}
                ${contentPanels.length ? `<section class="content-grid ${contentPanels.length === 1 ? "single" : ""}">${contentPanels.join("")}</section>` : ""}
                ${
                  hasRaw
                    ? `<details class="diagnostics" open>
                        <summary>
                          <span>${this.icon("mdi:database-search")} ${this.t("rawDp")}</span>
                          <small>${this.t("rawHint")}</small>
                        </summary>
                        <div class="table-wrap">
                          <table>
                            <thead>
                              <tr><th>${this.t("dp")}</th><th>${this.t("code")}</th><th>${this.t("rawApi")}</th><th>${this.t("scaledMeaning")}</th></tr>
                            </thead>
                            <tbody>${rawRows}</tbody>
                          </table>
                        </div>
                      </details>`
                    : ""
                }
              `
              : `<div class="empty-state">${this.icon("mdi:database-off")} ${this.t("noData")}</div>`
          }
        </div>
      </ha-card>
    `;

    if (this._style) {
      this.prepend(this._style);
    }

    this.querySelector(".power-button")?.addEventListener("click", () => this.toggleCharging());
    this.querySelector(".current-slider")?.addEventListener("change", (event) => {
      this.setCurrentLimit(event.target.value);
    });
    this.querySelector(".charging-mode")?.addEventListener("change", (event) => {
      this.setChargingMode(event.target.value);
    });
    this.querySelector(".target-energy")?.addEventListener("change", (event) => {
      this.setTargetEnergy(event.target.value);
    });
    this.querySelector(".schedule-start-time")?.addEventListener("change", (event) => {
      this.setScheduleBoundary(this.config.entities.scheduleStartTime || this.config.entities.scheduleTime, event.target.value);
    });
    this.querySelector(".schedule-end-time")?.addEventListener("change", (event) => {
      this.setScheduleBoundary(this.config.entities.scheduleEndTime, event.target.value);
    });
    this.querySelector(".planner-enabled")?.addEventListener("change", (event) => {
      this._plannerDraft.enabled = event.target.checked;
      this._plannerDirty = true;
      this._plannerSaved = false;
      this.render();
    });
    this.querySelector(".planner-add")?.addEventListener("click", () => this.addPlannerWindow());
    this.querySelector(".planner-save")?.addEventListener("click", () => this.savePlanner());
    this.querySelectorAll(".planner-window").forEach((row) => {
      const index = Number(row.dataset.window);
      row.querySelector(".planner-remove")?.addEventListener("click", () => {
        this._plannerDraft.windows.splice(index, 1);
        this._plannerDirty = true;
        this._plannerSaved = false;
        this.render();
      });
      row.querySelectorAll("[data-planner-day]").forEach((input) => {
        input.addEventListener("change", () => {
          const day = Number(input.dataset.plannerDay);
          const days = new Set(this._plannerDraft.windows[index].days || []);
          input.checked ? days.add(day) : days.delete(day);
          this._plannerDraft.windows[index].days = [...days].sort();
          this._plannerDirty = true;
          this._plannerSaved = false;
          this.render();
        });
      });
      row.querySelectorAll("[data-planner-field]").forEach((input) => {
        input.addEventListener("change", () => {
          const field = input.dataset.plannerField;
          this._plannerDraft.windows[index][field] = field === "current_a" ? Number(input.value) : input.value;
          this._plannerDirty = true;
          this._plannerSaved = false;
          this.render();
        });
      });
    });
    this.querySelectorAll("[data-planner-override]").forEach((button) => {
      button.addEventListener("click", () => {
        const mode = button.dataset.plannerOverride;
        const values = {};
        if (button.dataset.duration) values.duration_minutes = Number(button.dataset.duration);
        if (mode === "energy") {
          values.energy_kwh = Number(this.querySelector(".planner-energy-value")?.value || 0);
        }
        this.setPlannerOverride(mode, values);
      });
    });
  }

  connectedCallback() {
    if (!this._style) {
      this._style = document.createElement("style");
      this._style.textContent = `
        amperepoint-q22-card {
          display: block;
          container-type: inline-size;
          container-name: amperepoint-card;
        }
        .app {
          --ap-bg: #101214;
          --ap-panel: #171a1d;
          --ap-panel-2: #202329;
          --ap-border: rgba(255,255,255,.08);
          --ap-text: #f5f5f3;
          --ap-muted: #a6adb4;
          --ap-orange: #ff970f;
          --ap-orange-soft: rgba(255,151,15,.16);
          --ap-blue: #38a3ff;
          --ap-green: #35c46d;
          --ap-red: #ff5c5c;
          color: var(--ap-text);
        }
        ha-card {
          overflow: hidden;
          border: 1px solid var(--ap-border);
          border-radius: 18px;
          background:
            radial-gradient(circle at top left, rgba(255,151,15,.18), transparent 36rem),
            linear-gradient(135deg, #161819, #101214 52%, #151719);
          box-shadow: 0 18px 42px rgba(0,0,0,.28);
        }
        .app {
          padding: 22px;
          max-width: 1500px;
          margin: 0 auto;
        }
        .hero, .dashboard, .content-grid {
          display: grid;
          gap: 16px;
        }
        .hero {
          grid-template-columns: minmax(0, 1fr) auto;
          align-items: start;
          padding: 6px 2px 18px;
          border-bottom: 1px solid var(--ap-border);
        }
        .eyebrow {
          color: var(--ap-orange);
          font-size: 12px;
          font-weight: 800;
          letter-spacing: .04em;
          text-transform: uppercase;
        }
        h1 {
          margin: 5px 0 10px;
          font-size: clamp(30px, 3vw, 48px);
          line-height: 1;
          font-weight: 860;
          letter-spacing: 0;
        }
        .hero-meta {
          display: flex;
          flex-wrap: wrap;
          gap: 10px;
          color: var(--ap-muted);
          font-size: 13px;
        }
        .hero-meta span, .control-note {
          display: inline-flex;
          align-items: center;
          gap: 7px;
        }
        ha-icon {
          width: 18px;
          height: 18px;
          color: var(--ap-orange);
        }
        .hero-status {
          display: flex;
          flex-direction: column;
          align-items: flex-end;
          gap: 8px;
        }
        .pill {
          display: inline-flex;
          align-items: center;
          min-height: 34px;
          padding: 0 14px;
          border-radius: 999px;
          background: rgba(255,255,255,.06);
          border: 1px solid var(--ap-border);
          font-weight: 800;
          box-shadow: inset 0 0 0 1px rgba(255,255,255,.04);
        }
        .pill.charging {
          color: #171006;
          background: linear-gradient(135deg, #ffb13b, var(--ap-orange));
          border-color: rgba(255,151,15,.7);
          box-shadow: 0 12px 28px rgba(255,151,15,.2);
        }
        .hero-status small {
          color: var(--ap-muted);
          font-size: 12px;
        }
        .dashboard {
          grid-template-columns: minmax(320px, 1.25fr) minmax(300px, .95fr);
          margin-top: 18px;
        }
        .dashboard.single,
        .content-grid.single {
          grid-template-columns: 1fr;
        }
        .power-card, .control-card, .planner-card, .panel, .metric, .diagnostics, .empty-state {
          background: rgba(23,26,29,.88);
          border: 1px solid var(--ap-border);
          border-radius: 16px;
        }
        .power-card {
          display: grid;
          grid-template-columns: 280px minmax(0, 1fr);
          align-items: center;
          min-height: 290px;
          padding: 22px;
          gap: 20px;
        }
        .gauge {
          position: relative;
          width: min(260px, 100%);
          justify-self: center;
          aspect-ratio: 1;
          display: grid;
          place-items: center;
          border-radius: 50%;
          background: conic-gradient(from -140deg, var(--ap-orange) var(--power), rgba(255,255,255,.09) 0 78%, rgba(255,255,255,0) 78% 100%);
          box-shadow: inset 0 0 30px rgba(0,0,0,.42), 0 0 0 1px rgba(255,151,15,.08);
        }
        .gauge::before {
          content: "";
          position: absolute;
          inset: 24px;
          border-radius: inherit;
          background: linear-gradient(135deg, #171a1d, #111315);
          box-shadow: inset 0 0 34px rgba(0,0,0,.5);
        }
        .gauge div {
          position: relative;
          z-index: 1;
          text-align: center;
        }
        .gauge strong {
          display: block;
          font-size: clamp(42px, 5vw, 70px);
          line-height: .9;
          font-weight: 870;
        }
        .gauge span {
          display: block;
          margin-top: 8px;
          color: var(--ap-muted);
          font-size: 14px;
        }
        .session-strip {
          display: grid;
          gap: 12px;
        }
        .session-strip div {
          padding: 16px;
          border-radius: 14px;
          background: linear-gradient(135deg, rgba(255,151,15,.13), rgba(255,255,255,.035));
          border: 1px solid rgba(255,151,15,.14);
        }
        .session-strip span, .metric-label, .phase span, .status-row span, .panel-head span, .slider-label span {
          display: block;
          color: var(--ap-muted);
          font-size: 12px;
          font-weight: 700;
        }
        .session-strip b {
          display: block;
          margin-top: 5px;
          font-size: 26px;
        }
        .control-card {
          padding: 22px;
          display: flex;
          flex-direction: column;
          gap: 22px;
        }
        .card-title, .panel-head, .phase-head, .slider-label {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
        }
        .card-title strong, .panel-head strong {
          display: block;
          margin-top: 5px;
          font-size: 22px;
        }
        button {
          font: inherit;
        }
        .power-button {
          min-width: 120px;
          height: 48px;
          display: inline-flex;
          align-items: center;
          justify-content: center;
          gap: 8px;
          border: 0;
          border-radius: 999px;
          color: #15100a;
          background: linear-gradient(135deg, #ffc15c, var(--ap-orange));
          font-weight: 860;
          cursor: pointer;
          box-shadow: 0 12px 24px rgba(255,151,15,.2);
        }
        .power-button.on {
          color: #fff;
          background: linear-gradient(135deg, #2f3338, #202329);
          border: 1px solid var(--ap-border);
          box-shadow: none;
        }
        .power-button ha-icon {
          color: currentColor;
        }
        .current-slider {
          width: 100%;
          accent-color: var(--ap-orange);
        }
        .slider-scale {
          display: flex;
          justify-content: space-between;
          color: var(--ap-muted);
          font-size: 12px;
        }
        .control-note {
          margin-top: auto;
          padding: 14px;
          border-radius: 14px;
          background: rgba(56,163,255,.09);
          color: #c7e7ff;
          font-size: 13px;
          line-height: 1.45;
        }
        .control-note ha-icon {
          color: var(--ap-blue);
          flex: 0 0 auto;
        }
        .control-fields {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 12px;
        }
        .control-fields label {
          display: grid;
          gap: 7px;
          color: var(--ap-muted);
          font-size: 12px;
          font-weight: 750;
        }
        .control-fields select, .control-fields input {
          box-sizing: border-box;
          width: 100%;
          min-height: 42px;
          padding: 9px 11px;
          color: var(--ap-text);
          background: rgba(255,255,255,.06);
          border: 1px solid var(--ap-border);
          border-radius: 10px;
        }
        .control-fields select {
          color: #ffd08c;
          color-scheme: dark;
          background-color: #25282c;
          font-weight: 800;
        }
        .control-fields select option {
          color: #151719;
          background-color: #f5f5f3;
          font-weight: 700;
        }
        .control-fields select:focus-visible {
          outline: 2px solid var(--ap-orange);
          outline-offset: 2px;
        }
        .control-fields select:disabled {
          cursor: wait;
          opacity: .72;
        }
        .number-field {
          display: flex;
          align-items: center;
          gap: 7px;
        }
        .control-fields.single {
          grid-template-columns: 1fr;
        }
        .mode-pending {
          display: flex;
          align-items: center;
          gap: 8px;
          color: var(--ap-blue);
          font-size: 12px;
        }
        .schedule-card {
          display: grid;
          grid-template-columns: auto minmax(0, 1fr);
          align-items: start;
          gap: 12px;
          padding: 14px;
          border: 1px solid rgba(56,163,255,.2);
          border-radius: 14px;
          background: rgba(56,163,255,.09);
        }
        .schedule-card > ha-icon {
          color: var(--ap-blue);
        }
        .schedule-card strong, .schedule-card span {
          display: block;
        }
        .schedule-card span {
          margin-top: 5px;
          color: var(--ap-muted);
          font-size: 12px;
          line-height: 1.45;
        }
        .schedule-card label {
          display: grid;
          gap: 7px;
          margin-top: 10px;
        }
        .schedule-window {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 220px));
          gap: 10px;
        }
        .schedule-card .schedule-overnight {
          color: var(--ap-orange);
          font-weight: 700;
        }
        .schedule-card input {
          box-sizing: border-box;
          width: min(220px, 100%);
          min-height: 42px;
          padding: 9px 11px;
          color: var(--ap-text);
          color-scheme: dark;
          background: rgba(255,255,255,.06);
          border: 1px solid var(--ap-border);
          border-radius: 10px;
        }
        .metrics-grid {
          grid-column: 1 / -1;
          display: grid;
          grid-template-columns: repeat(4, minmax(0, 1fr));
          gap: 12px;
        }
        .planner-card {
          margin-top: 16px;
          padding: 20px;
        }
        .planner-head, .planner-actions {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 12px;
          flex-wrap: wrap;
        }
        .planner-head > div > span, .planner-summary small, .planner-field > span, .planner-override > div:first-child span {
          display: block;
          color: var(--ap-muted);
          font-size: 12px;
          font-weight: 700;
        }
        .planner-head strong {
          display: block;
          margin-top: 4px;
          font-size: 22px;
        }
        .planner-state {
          padding: 7px 11px;
          border: 1px solid var(--ap-border);
          border-radius: 999px;
          color: #c7e7ff;
          background: rgba(56,163,255,.09);
          font-size: 12px;
          font-weight: 800;
        }
        .planner-state.scheduled_charging, .planner-state.override_charging {
          color: #15100a;
          background: var(--ap-orange);
          border-color: var(--ap-orange);
        }
        .planner-state.failed, .planner-state.source_unavailable {
          color: #ffd0d0;
          background: rgba(255,92,92,.12);
          border-color: rgba(255,92,92,.3);
        }
        .planner-summary {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 10px;
          margin-top: 16px;
        }
        .planner-summary > div {
          display: flex;
          align-items: center;
          gap: 10px;
          min-width: 0;
          padding: 12px;
          border-radius: 12px;
          background: rgba(255,255,255,.035);
          border: 1px solid rgba(255,255,255,.055);
        }
        .planner-summary b {
          display: block;
          margin-top: 3px;
          overflow-wrap: anywhere;
        }
        .planner-toggle {
          display: inline-flex;
          align-items: center;
          gap: 9px;
          margin: 16px 0 12px;
          color: var(--ap-text);
          font-weight: 800;
          cursor: pointer;
        }
        .planner-toggle input, .day-chip input {
          accent-color: var(--ap-orange);
        }
        .planner-windows {
          display: grid;
          gap: 10px;
        }
        .planner-window {
          display: grid;
          grid-template-columns: minmax(300px, 1fr) minmax(110px, .34fr) minmax(110px, .34fr) minmax(110px, .3fr) 42px;
          align-items: end;
          gap: 10px;
          padding: 12px;
          border: 1px solid var(--ap-border);
          border-radius: 14px;
          background: rgba(0,0,0,.14);
        }
        .planner-days {
          display: flex;
          flex-wrap: wrap;
          align-items: center;
          gap: 5px;
          min-height: 42px;
        }
        .day-chip {
          position: relative;
          display: grid;
          place-items: center;
          min-width: 36px;
          height: 34px;
          padding: 0 5px;
          border: 1px solid var(--ap-border);
          border-radius: 9px;
          color: var(--ap-muted);
          background: rgba(255,255,255,.04);
          font-size: 11px;
          font-weight: 800;
          cursor: pointer;
        }
        .day-chip.selected {
          color: #15100a;
          background: #ffb13b;
          border-color: var(--ap-orange);
        }
        .day-chip input {
          position: absolute;
          opacity: 0;
          pointer-events: none;
        }
        .planner-field {
          display: grid;
          gap: 6px;
        }
        .planner-field input, .planner-energy input {
          box-sizing: border-box;
          width: 100%;
          min-height: 42px;
          padding: 8px 10px;
          color: var(--ap-text);
          color-scheme: dark;
          background: rgba(255,255,255,.06);
          border: 1px solid var(--ap-border);
          border-radius: 10px;
        }
        .planner-number, .planner-energy {
          display: flex;
          align-items: center;
          gap: 7px;
        }
        .planner-remove {
          width: 42px;
          height: 42px;
          display: grid;
          place-items: center;
          border: 1px solid rgba(255,92,92,.24);
          border-radius: 10px;
          color: var(--ap-red);
          background: rgba(255,92,92,.08);
          cursor: pointer;
        }
        .planner-remove ha-icon {
          color: currentColor;
        }
        .planner-empty {
          padding: 16px;
          border: 1px dashed var(--ap-border);
          border-radius: 12px;
          color: var(--ap-muted);
          text-align: center;
        }
        .planner-actions {
          margin-top: 12px;
        }
        .planner-actions button, .planner-override button {
          min-height: 40px;
          padding: 8px 13px;
          border: 1px solid var(--ap-border);
          border-radius: 10px;
          color: var(--ap-text);
          background: rgba(255,255,255,.06);
          font-weight: 800;
          cursor: pointer;
        }
        .planner-actions button {
          display: inline-flex;
          align-items: center;
          gap: 7px;
        }
        .planner-actions .planner-save {
          margin-left: auto;
          color: #15100a;
          background: var(--ap-orange);
          border-color: var(--ap-orange);
        }
        .planner-actions .planner-save ha-icon {
          color: currentColor;
        }
        .planner-save-state {
          color: var(--ap-orange);
          font-size: 12px;
          font-weight: 800;
        }
        .planner-override {
          display: flex;
          align-items: center;
          flex-wrap: wrap;
          gap: 8px;
          margin-top: 18px;
          padding-top: 16px;
          border-top: 1px solid var(--ap-border);
        }
        .planner-override > div:first-child {
          margin-right: 4px;
        }
        .planner-energy input {
          width: 76px;
        }
        .metric {
          padding: 16px;
          display: flex;
          align-items: center;
          gap: 14px;
        }
        .metric-icon {
          width: 42px;
          height: 42px;
          display: grid;
          place-items: center;
          flex: 0 0 auto;
          border-radius: 12px;
          background: rgba(255,255,255,.06);
        }
        .metric.accent .metric-icon {
          background: var(--ap-orange-soft);
        }
        .metric-value {
          margin-top: 4px;
          font-size: 24px;
          font-weight: 860;
        }
        .metric-value span {
          margin-left: 5px;
          color: var(--ap-muted);
          font-size: 13px;
          font-weight: 700;
        }
        .content-grid {
          grid-template-columns: 1.1fr .9fr;
          margin-top: 16px;
        }
        .panel {
          padding: 20px;
        }
        .panel-head {
          margin-bottom: 14px;
        }
        .panel-head em {
          color: var(--ap-orange);
          font-style: normal;
          font-size: 12px;
          font-weight: 800;
          text-transform: uppercase;
        }
        .phases-panel {
          display: grid;
          gap: 12px;
        }
        .phase {
          padding: 14px;
          border-radius: 14px;
          background: rgba(255,255,255,.035);
          border: 1px solid rgba(255,255,255,.055);
        }
        .phase.active {
          border-color: rgba(255,151,15,.25);
          background: rgba(255,151,15,.08);
        }
        .phase-head b {
          font-size: 20px;
        }
        .bar {
          height: 8px;
          overflow: hidden;
          margin: 12px 0;
          border-radius: 999px;
          background: rgba(255,255,255,.08);
        }
        .bar i {
          display: block;
          height: 100%;
          border-radius: inherit;
          background: linear-gradient(90deg, #ffba4c, var(--ap-orange));
        }
        .phase-grid {
          display: grid;
          grid-template-columns: repeat(2, 1fr);
          gap: 8px;
        }
        .phase-grid div {
          padding: 10px;
          border-radius: 10px;
          background: rgba(0,0,0,.18);
        }
        .phase-grid b {
          display: block;
          margin-top: 4px;
        }
        .status-row {
          display: grid;
          grid-template-columns: 40px minmax(0, 1fr) auto;
          align-items: center;
          gap: 12px;
          min-height: 45px;
          border-bottom: 1px solid rgba(255,255,255,.06);
        }
        .status-row:last-child {
          border-bottom: 0;
        }
        .status-row strong {
          text-align: right;
        }
        .row-icon {
          width: 34px;
          height: 34px;
          display: grid;
          place-items: center;
          margin-right: 2px;
          border-radius: 10px;
          background: rgba(255,255,255,.045);
        }
        .status-row.good .row-icon ha-icon {
          color: var(--ap-green);
        }
        .status-row.bad .row-icon ha-icon {
          color: var(--ap-red);
        }
        .diagnostics {
          margin-top: 16px;
          padding: 0;
        }
        .diagnostics summary {
          display: flex;
          align-items: center;
          justify-content: space-between;
          gap: 16px;
          padding: 16px 18px;
          cursor: pointer;
          list-style: none;
        }
        .diagnostics summary::-webkit-details-marker {
          display: none;
        }
        .diagnostics summary span {
          display: inline-flex;
          align-items: center;
          gap: 8px;
          font-weight: 800;
        }
        .diagnostics summary small {
          color: var(--ap-muted);
        }
        .table-wrap {
          overflow-x: auto;
          border-top: 1px solid var(--ap-border);
        }
        table {
          width: 100%;
          border-collapse: collapse;
          font-size: 13px;
        }
        th, td {
          padding: 10px 12px;
          border-bottom: 1px solid rgba(255,255,255,.055);
          text-align: left;
          white-space: nowrap;
        }
        th {
          color: var(--ap-muted);
          font-size: 11px;
          text-transform: uppercase;
        }
        code {
          padding: 3px 6px;
          border-radius: 6px;
          background: rgba(0,0,0,.22);
          color: #f4d6a7;
        }
        .empty-state {
          margin-top: 18px;
          padding: 20px;
          display: flex;
          align-items: center;
          gap: 10px;
          color: var(--ap-muted);
        }
        @media (max-width: 1100px) {
          .dashboard, .content-grid, .power-card {
            grid-template-columns: 1fr;
          }
          .metrics-grid {
            grid-template-columns: repeat(2, minmax(0, 1fr));
          }
          .planner-window {
            grid-template-columns: minmax(280px, 1fr) repeat(3, minmax(100px, .35fr)) 42px;
          }
        }
        @media (max-width: 680px) {
          .app {
            padding: 14px;
          }
          .hero {
            grid-template-columns: 1fr;
          }
          .hero-status {
            align-items: flex-start;
          }
          .metrics-grid {
            grid-template-columns: 1fr;
          }
          .control-fields {
            grid-template-columns: 1fr;
          }
          .schedule-window {
            grid-template-columns: 1fr;
          }
          .control-card .card-title {
            align-items: stretch;
            flex-direction: column;
          }
          .power-button {
            width: 100%;
          }
          .diagnostics summary {
            align-items: flex-start;
            flex-wrap: wrap;
          }
          .diagnostics summary small {
            flex: 1 0 100%;
            overflow-wrap: anywhere;
          }
          .status-row {
            grid-template-columns: 36px minmax(0, 1fr);
          }
          .status-row strong {
            grid-column: 2;
            text-align: left;
          }
          .planner-summary, .planner-window {
            grid-template-columns: 1fr;
          }
          .planner-remove {
            width: 100%;
          }
          .planner-actions .planner-save {
            margin-left: 0;
          }
          .planner-actions > * {
            flex: 1 1 100%;
          }
          .planner-override > button, .planner-energy {
            width: 100%;
          }
          .planner-energy button {
            flex: 1;
          }
        }
        @container amperepoint-card (max-width: 1100px) {
          .dashboard, .content-grid, .power-card {
            grid-template-columns: 1fr;
          }
          .metrics-grid {
            grid-template-columns: repeat(2, minmax(0, 1fr));
          }
          .planner-window {
            grid-template-columns: minmax(280px, 1fr) repeat(3, minmax(100px, .35fr)) 42px;
          }
        }
        @container amperepoint-card (max-width: 680px) {
          .app {
            padding: 14px;
          }
          .hero {
            grid-template-columns: 1fr;
          }
          .hero-status {
            align-items: flex-start;
          }
          .metrics-grid, .control-fields, .schedule-window {
            grid-template-columns: 1fr;
          }
          .control-card .card-title {
            align-items: stretch;
            flex-direction: column;
          }
          .power-button {
            width: 100%;
          }
          .diagnostics summary {
            align-items: flex-start;
            flex-wrap: wrap;
          }
          .diagnostics summary small {
            flex: 1 0 100%;
            overflow-wrap: anywhere;
          }
          .status-row {
            grid-template-columns: 36px minmax(0, 1fr);
          }
          .status-row strong {
            grid-column: 2;
            text-align: left;
          }
          .planner-summary, .planner-window {
            grid-template-columns: 1fr;
          }
          .planner-remove {
            width: 100%;
          }
          .planner-actions .planner-save {
            margin-left: 0;
          }
          .planner-actions > * {
            flex: 1 1 100%;
          }
          .planner-override > button, .planner-energy {
            width: 100%;
          }
          .planner-energy button {
            flex: 1;
          }
        }
      `;
      this.appendChild(this._style);
    }
  }
}

if (!customElements.get("amperepoint-q22-card")) {
  customElements.define("amperepoint-q22-card", AmperePointQ22Card);
}

window.customCards = window.customCards || [];
window.customCards.push({
  type: "amperepoint-q22-card",
  name: "AmperePoint Q Series",
  description: "AmperePoint Q Series EV charger dashboard card",
});
